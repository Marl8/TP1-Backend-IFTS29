doctype html
html    
  head
    title= title
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1")
    // Bootstrap CSS
    link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css")
    link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css")
    style.
      html, body {
        height: 100%;
      }
      body {
        display: flex;
        flex-direction: column;
        min-height: 100vh;
        background: #f8f9fa;
        font-family: Arial, sans-serif;
      }
      .card {
        height: 170px;
        max-width: 350px;
        border-radius: 15px;
        transition: transform 0.2s;
        word-wrap: break-word;
        overflow-wrap: break-word;
        margin: 0 auto;
      }
      .card.fluid{
        height: auto;
        max-width: 600px; 
        margin: 0 auto;
        border-radius:15px;
        background-color: #2c2f33;
         color: #fff;
      }
      .card:hover {
        transform: scale(1.05);
      }
      .card-body{
        display: flex;
        flex-direction: column;
      }
      .mt-auto{
        margin-top: auto;
      }
      .header {
        background: #343a40;
        color: white;
        padding: 20px;
        text-align: center;
        border-radius: 0 0 20px 20px;
      }
      main{
        flex: 1;
      }
      .btn-sabor {
        background-color: #2c3e50;
        color: #fff;
        border: none;
        transition: 0.3s ease;
        width: 200px;
      }

      .btn-sabor:hover {
        background-color: #1a252f;
      }

      .btn-crud {
        background-color: #343a40; /* gris oscuro elegante */
        color: #fff;
        border: none;
        width: 50%;               /* ancho uniforme */
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0.75rem auto;     /* centrado y espaciado vertical */
        padding: 0.6rem 1rem;     /* altura consistente */
        border-radius: 6px;
        font-weight: 500;
        text-decoration: none;    /* elimina subrayado si es <a> */
        transition: background-color 0.3s ease, transform 0.1s ease;
      }

      .btn-crud:hover {
        background-color: #212529; /* tono más oscuro al pasar */
        color: #f8f9fa;
        transform: scale(1.02);
      }

      .btn-crud i {
        margin-right: 8px; /* separa el icono del texto */
        font-size: 1rem;
      }
      .footer {
        background: #343a40;
        color: white;
        border-radius: 20px 20px 0 0;
      }
      .footer a {
        color: #ffc107;
        text-decoration: none;
      }
      .footer a:hover {
        text-decoration: underline;
      }
      .footer p {
        margin: 0;
      }
  body
    .header
      h1 Sabor Urbano
      p Bienvenido al sistema de gestión

    main.container.mt-5
      block content

    footer.footer.mt-5
      .container.text-center.py-4
        h5.mb-4(style="color:#ffffff; font-weight:600; letter-spacing:1px;") NRE Studio
        p.mb-1(style="color:#f1f1f1;") &copy; 2025 Net Remote Environment. Todos los derechos reservados.
        hr.my-2(style="border-color: rgba(255,255,255,0.3); width:80%; margin:auto;")
        p.mt-2(style="color:#d3d3d3; font-size:0.9rem;") Proyecto académico desarrollado por el grupo N° 8 del IFTS N° 29.
    // Bootstrap JS
    script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js")
