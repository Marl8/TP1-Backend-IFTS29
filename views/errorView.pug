extends layout

block content
  .container.text-center.mt-5
    .card.fluid.shadow.p-4.text-center
      h1.display-6.mb-3(style="font-weight:700; color:#ff4d4d;") Error del Servidor
      p.lead.mb-4 Ha ocurrido un error inesperado al procesar la solicitud.
      if message
        p.mb-4
          strong(style="color:#f1f1f1") Mensaje:&nbsp;
          | #{message}
      a.btn.btn-light.mt-2(href="/") ← Volver al Inicio